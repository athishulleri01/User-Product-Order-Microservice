# email_service/email/urls.py
from django.urls import path

urlpatterns = [
    # Define your email service URLs if needed
    # For example, an endpoint to trigger the email sending process
    # path('send-email/', your_view_function, name='send-email'),
]
